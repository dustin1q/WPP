---
/*
export async function getStaticPaths() {
  const posts = await Astro.glob('samples/*.md')

  return posts.map(post => ({
    params: {
      sample: post.frontmatter.sample
    },
    props: {
      post
    },
  }))
}
*/



export const getStaticPaths = async () => {
    
    const samples = await Astro.glob('./samples/*.md')
  return samples.map(({title}) => {
    return {
        params: {
           // sample: title
            sample: post.frontmatter.sample
        },
       // props: {title, desc, html, css, js},
    }
  });
}
const {title, desc, html, css, js} = Astro.props; 



const { Content } = Astro.props.post
---
<article>
  <Content/>
</article>